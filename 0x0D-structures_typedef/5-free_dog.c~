#include "holberton.h"
#include <stdlib.h>

/**
 * free_dog - free the dogs
 *
 * @s: input string of dog
 */
void free_dog(dog_t *d)
{
	if (d != NULL)
	{
		free(d->name);
		free(d->owner);
		free(d);
	}
}
